{
  "extends": "./tsconfig.json",

  "compilerOptions": {
    "rootDir": "src",
    "outDir": "../dist/backend"
  },

  "include": [
    "src/**/*.ts"
  ],

  "exclude": [
    "node_modules",
    "test",
    "dist",
    "**/*.spec.ts"
  ],

  "references": [
    {
      "path": "../contracts"
    }
  ]
}
